--- a/drivers/irqchip/Kconfig
+++ b/drivers/irqchip/Kconfig
@@ -317,6 +317,15 @@
 	select IRQ_DOMAIN_HIERARCHY
 	select MIPS_CM
 
+config RTL819X_INTC
+	bool "Realtek RTL819x Interrupt Controller"
+	depends on MIPS || COMPILE_TEST
+	select IRQ_DOMAIN
+	help
+	  Support for the interrupt controller on Realtek RTL819x SoCs
+	  (RTL8196E, RTL8197D, etc.). This controller manages peripheral
+	  interrupts including UART, Ethernet switch, timers, and others.
+
 config INGENIC_IRQ
 	bool
 	depends on MACH_INGENIC
