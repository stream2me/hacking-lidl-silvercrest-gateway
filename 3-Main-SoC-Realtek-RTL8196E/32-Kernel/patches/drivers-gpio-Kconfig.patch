--- a/drivers/gpio/Kconfig
+++ b/drivers/gpio/Kconfig
@@ -488,6 +488,11 @@
 	select GPIOLIB_IRQCHIP
 	help
 	  Say Y here to support RDA Micro GPIO controller.
+	  
+config GPIO_RTL819X
+	bool "Realtek RTL819x GPIO support"
+	depends on MIPS && OF_GPIO
+	default y
 
 config GPIO_REG
 	bool
