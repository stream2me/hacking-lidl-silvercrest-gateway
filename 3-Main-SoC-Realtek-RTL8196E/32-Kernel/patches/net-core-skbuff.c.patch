--- a/net/core/skbuff.c
+++ b/net/core/skbuff.c
@@ -89,6 +89,10 @@
 int sysctl_max_skb_frags __read_mostly = MAX_SKB_FRAGS;
 EXPORT_SYMBOL(sysctl_max_skb_frags);
 
+/* Realtek private buffer pool support - vendor patch from 2.6.30 */
+extern int is_rtl865x_eth_priv_buf(unsigned char *head);
+extern void free_rtl865x_eth_priv_buf(unsigned char *head);
+
 /**
  *	skb_panic - private function for out-of-line support
  *	@skb:	buffer
@@ -600,10 +604,14 @@
 {
 	unsigned char *head = skb->head;
 
-	if (skb->head_frag)
+	if (skb->head_frag) {
 		skb_free_frag(head);
-	else
-		kfree(head);
+	} else {
+		if (is_rtl865x_eth_priv_buf(head))
+			free_rtl865x_eth_priv_buf(head);  /* Realtek vendor patch: return to private pool */
+		else
+			kfree(head);
+	}
 }
 
 static void skb_release_data(struct sk_buff *skb)
